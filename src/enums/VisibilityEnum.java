package enums;

/**
 * Defines visibility states for projects and other displayable items in the system.
 * Controls whether items should be shown to users or kept hidden.
 */
public enum VisibilityEnum {
	/** Project is visible to applicants and can be viewed in listings */
	VISIBLE,
	/** Project is hidden from view and not accessible to applicants */
	HIDDEN
}